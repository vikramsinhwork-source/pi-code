# Optional overrides — pi-code/.env is loaded first.
# Add secrets here if you prefer not to put them in the repo root .env

# Must match DEVICE_TOKEN_SECRET in railway-monitor backend
DEVICE_TOKEN_SECRET=

SOCKET_URL=https://railwaymonitor.in
