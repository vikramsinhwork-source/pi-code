#!/usr/bin/env node
/**
 * Regenerates docs/go2rtc.example.yaml content — prefer copying that file to Pi.
 */
console.log(`# Run: cp docs/go2rtc.example.yaml ~/go2rtc.yaml && sudo systemctl restart go2rtc`);
